spring:
  application:
    name: 02-persistencia
  datasource:
    url: jdbc:h2:mem:tasks;MODE=PostgreSQL
    username: sa
    password:
  flyway:
    enabled: true
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
