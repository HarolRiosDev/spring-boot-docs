spring:
  datasource:
    url: jdbc:h2:file:./data/tasks_observability;MODE=PostgreSQL
    username: sa
    password:
  cache:
    type: simple

management:
  health:
    redis:
      # sin Redis corriendo, su health check daría DOWN y arrastraría el estado global
      enabled: false
